spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mydb.ct0046mcugu4.ap-northeast-2.rds.amazonaws.com:3306/mydb
    username: root
    password: 3382149m!
  data:
   redis:
    host: redis  # 기본값 localhost
    port: 6379       # 기본값 6379

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true